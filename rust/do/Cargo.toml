[package]
name = "do"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1.12.3"
rust-ini = "0.21.3"

 [profile.release]
 strip = true
 opt-level = "z" # or "s"
 lto = true
 codegen-units = 1
 panic = "abort"

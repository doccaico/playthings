package lexbor

// tag module

import "core:c"

// Define

lxb_tag_id_t :: c.uintptr_t

// Fucntions

@(default_calling_convention = "c")
foreign lib {
	// ClearBackground :: proc(color: Color) ---
}
